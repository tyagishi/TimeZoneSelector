import XCTest
@testable import TimeZoneSelector

final class TimeZoneSelectorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TimeZoneSelector().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
